# 🤝 Contributing to RAMS Game

Спасибо за интерес к проекту! Мы рады любому вкладу в развитие игры.

## 📋 Содержание

- [Как внести вклад](#как-внести-вклад)
- [Рабочий процесс](#рабочий-процесс)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Коммит-сообщения](#коммит-сообщения)

## 🚀 Как внести вклад

### 1. Fork репозитория

```bash
# Форкните репозиторий через GitHub UI
# Затем клонируйте свой форк
git clone https://github.com/YOUR-USERNAME/RAMS.git
cd RAMS
```

### 2. Создайте ветку для вашей фичи

```bash
git checkout -b feature/amazing-feature
# или
git checkout -b fix/bug-fix
```

### 3. Установите зависимости

```bash
npm install
```

### 4. Внесите изменения

Следуйте нашим [стандартам кода](#стандарты-кода).

### 5. Протестируйте

```bash
npm test
npm run lint
```

### 6. Закоммитьте изменения

```bash
git add .
git commit -m "feat: add amazing feature"
```

См. [Коммит-сообщения](#коммит-сообщения) для деталей.

### 7. Отправьте в ваш форк

```bash
git push origin feature/amazing-feature
```

### 8. Создайте Pull Request

Откройте PR в основной репозиторий через GitHub UI.

## 🔄 Рабочий процесс

### Структура веток

- `main` - стабильная версия
- `develop` - разработка
- `feature/*` - новые фичи
- `fix/*` - исправления багов
- `hotfix/*` - срочные исправления
- `release/*` - подготовка релизов

### Процесс разработки

1. **Feature branches** от `develop`
2. **Pull Request** в `develop`
3. **Code Review** от мейнтейнеров
4. **Merge** после одобрения
5. **Release** через `release/*` в `main`

## 💻 Стандарты кода

### TypeScript

```typescript
// ✅ Хорошо
interface Player {
  id: number;
  name: string;
  balance: number;
}

const player: Player = {
  id: 1,
  name: 'Player 1',
  balance: 1000,
};

// ❌ Плохо
const player = {
  id: 1,
  name: 'Player 1',
  balance: 1000,
};
```

### React Components

```typescript
// ✅ Хорошо - функциональный компонент с типами
interface MenuScreenProps {
  navigation: NavigationProp<any>;
}

export default function MenuScreen({ navigation }: MenuScreenProps) {
  return <View>...</View>;
}

// ❌ Плохо - без типов
export default function MenuScreen({ navigation }) {
  return <View>...</View>;
}
```

### Именование

- **Компоненты**: `PascalCase` (MenuScreen.tsx)
- **Функции**: `camelCase` (getUserBalance)
- **Константы**: `UPPER_SNAKE_CASE` (MAX_BET_AMOUNT)
- **Интерфейсы**: `PascalCase` с префиксом I (IPlayer) или без
- **Типы**: `PascalCase` (GamePhase)

### Структура файлов

```typescript
// 1. Импорты
import React from 'react';
import { View, Text } from 'react-native';

// 2. Типы/Интерфейсы
interface Props {
  // ...
}

// 3. Константы
const CARD_WIDTH = 100;

// 4. Компонент
export default function Component({ prop }: Props) {
  // 4.1. Хуки
  const [state, setState] = useState();
  
  // 4.2. Эффекты
  useEffect(() => {}, []);
  
  // 4.3. Функции
  const handleClick = () => {};
  
  // 4.4. Render
  return <View />;
}

// 5. Стили
const styles = StyleSheet.create({});
```

### Комментарии

```typescript
// ✅ Хорошо - описание сложной логики
/**
 * Определяет победителя взятки на основе карт на столе
 * @param cards - карты на столе
 * @param trump - козырная масть
 * @returns индекс победившего игрока
 */
export function determineTrickWinner(
  cards: TableCard[],
  trump: Suit
): number {
  // Логика...
}

// ❌ Плохо - очевидные комментарии
// Увеличиваем счетчик на 1
counter += 1;
```

## 🧪 Тестирование

### Unit тесты

```typescript
// __tests__/gameLogic.test.ts
import { determineTrickWinner } from '../src/utils/gameLogic';

describe('gameLogic', () => {
  it('should determine winner correctly', () => {
    const cards = [/* ... */];
    const trump = 'hearts';
    const winner = determineTrickWinner(cards, trump);
    expect(winner).toBe(0);
  });
});
```

### Запуск тестов

```bash
# Все тесты
npm test

# В watch режиме
npm test -- --watch

# С покрытием
npm test -- --coverage
```

## 📝 Коммит-сообщения

Используем [Conventional Commits](https://www.conventionalcommits.org/):

### Формат

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы

- `feat`: новая фича
- `fix`: исправление бага
- `docs`: изменения в документации
- `style`: форматирование, отступы (не CSS)
- `refactor`: рефакторинг кода
- `perf`: улучшение производительности
- `test`: добавление тестов
- `chore`: обслуживание (обновление зависимостей и т.д.)

### Примеры

```bash
# Новая фича
git commit -m "feat(game): add betting modal with validation"

# Исправление бага
git commit -m "fix(wallet): resolve TON connection timeout"

# Документация
git commit -m "docs(readme): update installation instructions"

# Рефакторинг
git commit -m "refactor(store): migrate to Zustand from Redux"

# Производительность
git commit -m "perf(game): optimize card rendering with memo"
```

### Детальное описание

```bash
git commit -m "feat(wallet): add TON deposit functionality

- Implement depositFunds method in TonWalletService
- Add deposit UI in WalletScreen
- Add transaction validation
- Add loading states and error handling

Closes #123"
```

## 🐛 Сообщения об ошибках

При создании issue укажите:

### Шаблон Bug Report

```markdown
## Описание
Краткое описание проблемы.

## Шаги воспроизведения
1. Перейти в '...'
2. Нажать на '...'
3. Ошибка появляется

## Ожидаемое поведение
Что должно происходить.

## Фактическое поведение
Что происходит на самом деле.

## Скриншоты
Если применимо.

## Окружение
- OS: [Android 13 / iOS 16]
- Device: [Pixel 7 / iPhone 14]
- App version: [1.0.0]
- React Native version: [0.73.0]

## Дополнительная информация
Логи, stack traces и т.д.
```

## 💡 Предложения фич

### Шаблон Feature Request

```markdown
## Проблема
Описание проблемы, которую решает фича.

## Предлагаемое решение
Как вы видите решение.

## Альтернативы
Другие варианты, которые вы рассматривали.

## Дополнительный контекст
Скриншоты, примеры и т.д.
```

## 📚 Приоритетные области для вклада

### 🔥 High Priority

- [ ] Мультиплеер через WebSocket
- [ ] Смарт-контракт для TON
- [ ] Unit/Integration тесты
- [ ] E2E тесты
- [ ] Accessibility улучшения

### ⭐ Medium Priority

- [ ] Рейтинговая система
- [ ] Достижения
- [ ] Статистика игрока
- [ ] Анимации переходов
- [ ] Локализация (i18n)

### 💎 Low Priority

- [ ] Темы оформления
- [ ] Кастомизация аватаров
- [ ] Звуковые эффекты
- [ ] Музыка в меню
- [ ] Easter eggs

## 🎨 Дизайн

### UI/UX

- Следуйте Material Design (Android) и Human Interface Guidelines (iOS)
- Используйте существующую цветовую схему
- Обеспечьте touch-friendly размеры (минимум 44x44px)
- Добавляйте тактильную обратную связь (haptics)

### Цветовая палитра

```typescript
const colors = {
  primary: '#6a11cb',
  secondary: '#2575fc',
  success: '#4CAF50',
  warning: '#FFC107',
  error: '#f44336',
  background: '#1a1a2e',
  surface: '#16213e',
  text: '#ffffff',
  textSecondary: '#b0b0b0',
};
```

## 🔍 Code Review

Ваш PR будет проверен на:

- ✅ Соответствие стандартам кода
- ✅ Наличие тестов
- ✅ Отсутствие breaking changes
- ✅ Производительность
- ✅ Безопасность
- ✅ Документация
- ✅ Доступность (accessibility)

## 📞 Связь

- **Telegram**: @rams_game
- **Discord**: discord.gg/rams
- **Email**: dev@rams-game.com

## 📜 Лицензия

Внося вклад, вы соглашаетесь, что ваш код будет лицензирован под MIT License.

---

Спасибо за вклад в RAMS Game! 🎮🚀
